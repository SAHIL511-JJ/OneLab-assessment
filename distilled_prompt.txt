Build a complete payment reconciliation assessment project where a payments company has platform transaction records and bank settlement records that do not fully match at month end. Generate your own synthetic CSV data, plant realistic discrepancy cases like cross-month settlements, rounding mismatches, duplicate entries, and orphan refunds, then reconcile the two datasets, produce a JSON report, include tests, and show results in a simple frontend dashboard. The dashboard must work with built-in sample data and uploaded CSVs from a tester, so do not hardcode column names; add a column-mapping step for the required fields, auto-suggest likely matches, allow manual override, and add strong error handling for missing columns, invalid dates, invalid amounts, and unusable rows. Keep it as a static deployable frontend with Python scripts for data generation and reconciliation, and include assumptions, README instructions, and a short note on production limitations.
